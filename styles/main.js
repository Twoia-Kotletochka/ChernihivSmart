import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
    container: {
        flex: 1,
    },
    textStyle: {
        textAlign: 'center',
        color: 'black',
        fontSize: 18,
        padding: 20,
    },
    header: {
        justifyContent: 'center',
        alignItems: 'center',
        left: 0,
        right: 0,
        position: 'absolute'
    }
})
